<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Recent Activities</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/global.css" />
  </head>
  <body class="bg-gray-50 min-h-screen">
    <div class="flex h-screen bg-white">
      <%- include('partials/sidebar') %>
      <main class="flex-1 overflow-y-auto lg:ml-0">
        <%- include('partials/header', { title: 'All Recent Activities' }) %>
        <div class="p-4 lg:p-6">
          <div class="mb-8 lg:mb-10">
            <div class="relative">
              <!-- Background decoration -->
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/10 to-blue-500/10 rounded-3xl transform -rotate-1"
              ></div>
              <div class="relative glass-card rounded-2xl p-6 lg:p-8">
                <div
                  class="flex flex-col lg:flex-row lg:items-center lg:justify-between"
                >
                  <div class="flex items-center gap-4 mb-4 lg:mb-0">
                    <div class="relative">
                      <div
                        class="p-3 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-xl text-white shadow-lg"
                      >
                        <i class="fas fa-clipboard-list text-xl"></i>
                      </div>
                      <div class="pulse-ring"></div>
                    </div>
                    <div>
                      <h1
                        class="text-3xl lg:text-4xl font-bold bg-gradient-to-r from-gray-900 to-gray-600 bg-clip-text text-transparent"
                      >
                        All Recent Activities
                      </h1>
                      <p class="text-gray-600 mt-1 text-lg">
                        Complete log of all entries and exits
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="overflow-x-auto">
            <table
              class="min-w-full bg-white rounded-lg shadow-sm border border-gray-200"
            >
              <thead>
                <tr class="bg-gray-50">
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    #
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    Name
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    CNIC
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    Type
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    Entry Time
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    Exit Time
                  </th>
                  <th
                    class="px-4 py-2 text-left text-xs font-semibold text-gray-600"
                  >
                    Remarks
                  </th>
                </tr>
              </thead>
              <tbody>
                <% if (recentActivities && recentActivities.length > 0) { %> <%
                recentActivities.forEach(function(activity, idx) { %>
                <tr class="border-b hover:bg-gray-50">
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <%= idx + 1 %>
                  </td>
                  <td class="px-4 py-2 text-sm text-gray-800 font-medium">
                    <%= activity.name %>
                  </td>
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <%= activity.cnic %>
                  </td>
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <span
                      class="inline-flex items-center px-2 py-1 rounded text-xs font-semibold <%= activity.entry_type === 'ENTRY' ? 'bg-blue-100 text-blue-700' : 'bg-red-100 text-red-700' %>"
                    >
                      <%= activity.entry_type === 'ENTRY' ? 'Entry' : 'Exit' %>
                    </span>
                  </td>
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <%= activity.entry_time ? new
                    Date(activity.entry_time).toLocaleString() : '-' %>
                  </td>
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <%= activity.exit_time ? new
                    Date(activity.exit_time).toLocaleString() : '-' %>
                  </td>
                  <td class="px-4 py-2 text-xs text-gray-500">
                    <%= activity.remarks || '-' %>
                  </td>
                </tr>
                <% }); %> <% } else { %>
                <tr>
                  <td colspan="7" class="px-4 py-8 text-center text-gray-500">
                    <i class="fas fa-clock text-2xl mb-2"></i>
                    <span class="text-sm lg:text-base">No recent activity</span>
                  </td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <a
            href="/dashboard"
            class="mt-8 inline-block text-blue-600 hover:text-blue-700 font-medium"
            >Back to Dashboard</a
          >
        </div>
      </main>
    </div>
    <%- include('partials/toast') %>
  </body>
</html>
