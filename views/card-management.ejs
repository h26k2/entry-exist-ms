<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Card Management - Entry/Exit Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "#1e40af",
              secondary: "#3b82f6",
              accent: "#06b6d4",
              dark: "#1f2937",
              light: "#f8fafc",
            },
          },
        },
      };
    </script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/modals.css" />
    <style>
      .qr-code-preview {
        max-width: 120px;
        max-height: 120px;
      }
      .card-status-badge {
        padding: 0.25rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 500;
      }
      .status-active {
        background-color: #dcfce7;
        color: #166534;
      }
      .status-inactive {
        background-color: #f3f4f6;
        color: #374151;
      }
      .status-lost {
        background-color: #fee2e2;
        color: #991b1b;
      }
      .status-damaged {
        background-color: #fef3c7;
        color: #92400e;
      }
      .stat-card {
        background-color: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(8px);
        border-radius: 0.75rem;
        padding: 1rem 1.5rem;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.2);
        transition: all 0.3s ease;
      }
      .stat-card:hover {
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }
      @media (min-width: 1024px) {
        .stat-card {
          padding: 1.5rem;
        }
      }
    </style>
  </head>
  <body class="bg-gradient-to-br from-cyan-50 via-blue-50 to-indigo-50 min-h-screen">
    <div class="flex h-screen">
      <%- include('partials/sidebar') %>

      <main class="flex-1 overflow-y-auto">
        <%- include('partials/header', { title: 'Card Management' }) %>

        <!-- Main Content -->
        <div class="p-4 lg:p-6 animate-fade-in">
          <!-- Page Header with Enhanced Design -->
          <div class="mb-8 lg:mb-10">
            <div class="relative">
              <!-- Background decoration -->
              <div
                class="absolute inset-0 bg-gradient-to-r from-cyan-500/10 to-blue-500/10 rounded-3xl transform -rotate-1"
              ></div>
              <div class="relative glass-card rounded-2xl p-6 lg:p-8">
                <div
                  class="flex flex-col lg:flex-row lg:items-center lg:justify-between"
                >
                  <div class="flex items-center gap-4 mb-4 lg:mb-0">
                    <div class="relative">
                      <div
                        class="p-3 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-xl text-white shadow-lg"
                      >
                        <i class="fas fa-id-card text-xl"></i>
                      </div>
                      <div class="pulse-ring"></div>
                    </div>
                    <div>
                      <h1
                        class="text-3xl lg:text-4xl font-bold bg-gradient-to-r from-gray-900 to-gray-600 bg-clip-text text-transparent"
                      >
                        Card Management
                      </h1>
                      <p class="text-gray-600 mt-1 text-lg">
                        Manage entry cards, QR codes, and access control
                      </p>
                    </div>
                  </div>

                  <!-- Action Buttons and Live Status -->
                  <div class="flex items-center gap-4">
                    <button
                      onclick="openCreateCardModal()"
                      class="bg-gradient-to-r from-green-500 to-emerald-600 text-white px-6 py-3 rounded-xl hover:from-green-600 hover:to-emerald-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 flex items-center gap-2"
                    >
                      <i class="fas fa-plus text-sm"></i>
                      <span class="hidden sm:inline">Create New Card</span>
                      <span class="sm:hidden">New Card</span>
                    </button>
                    
                    <button
                      onclick="cardManagement.downloadQRCodesAsZip()"
                      class="bg-gradient-to-r from-purple-500 to-indigo-600 text-white px-6 py-3 rounded-xl hover:from-purple-600 hover:to-indigo-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 flex items-center gap-2"
                      title="Download all QR codes as ZIP"
                    >
                      <i class="fas fa-download text-sm"></i>
                      <span class="hidden sm:inline">Download All QR</span>
                      <span class="sm:hidden">Download</span>
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Quick Stats -->
          <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6 mb-8">
            <div class="stat-card">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm font-medium text-gray-600 mb-1">
                    Total Cards
                  </p>
                  <p
                    class="text-2xl lg:text-3xl font-bold text-cyan-600"
                    id="totalCards"
                  >
                    0
                  </p>
                </div>
                <div
                  class="p-3 bg-gradient-to-br from-cyan-500 to-cyan-600 rounded-xl text-white shadow-lg"
                >
                  <i class="fas fa-id-card text-xl"></i>
                </div>
              </div>
            </div>

              <div class="stat-card">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-sm font-medium text-gray-600 mb-1">
                      Lost Cards
                    </p>
                    <p
                      class="text-2xl lg:text-3xl font-bold text-red-600"
                      id="lostCards"
                    >
                      0
                    </p>
                  </div>
                  <div
                    class="p-3 bg-gradient-to-br from-red-500 to-red-600 rounded-xl text-white shadow-lg"
                  >
                    <i class="fas fa-exclamation-circle text-xl"></i>
                  </div>
                </div>
              </div>

            <div class="stat-card">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm font-medium text-gray-600 mb-1">
                    Active Cards
                  </p>
                  <p
                    class="text-2xl lg:text-3xl font-bold text-green-600"
                    id="activeCards"
                  >
                    0
                  </p>
                </div>
                <div
                  class="p-3 bg-gradient-to-br from-green-500 to-green-600 rounded-xl text-white shadow-lg"
                >
                  <i class="fas fa-check-circle text-xl"></i>
                </div>
              </div>
            </div>


            <div class="stat-card">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-sm font-medium text-gray-600 mb-1">
                    Unique People
                  </p>
                  <p
                    class="text-2xl lg:text-3xl font-bold text-purple-600"
                    id="uniquePeople"
                  >
                    0
                  </p>
                </div>
                <div
                  class="p-3 bg-gradient-to-br from-purple-500 to-purple-600 rounded-xl text-white shadow-lg"
                >
                  <i class="fas fa-users text-xl"></i>
                </div>
              </div>
            </div>
          </div>

          <!-- Filters and Search -->
          <div class="glass-card rounded-2xl p-6 lg:p-8 mb-8">
            <div class="flex items-center gap-3 mb-6">
              <div
                class="p-2 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-lg text-white"
              >
                <i class="fas fa-filter text-sm"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900">
                Search & Filter Cards
              </h3>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4 lg:gap-6">
              <div>
                <label
                  for="searchInput"
                  class="block text-sm font-medium text-gray-700 mb-2"
                  >Search Cards</label
                >
                <div class="relative">
                  <div
                    class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
                  >
                    <i class="fas fa-search text-gray-400 text-sm"></i>
                  </div>
                  <input
                    type="text"
                    id="searchInput"
                    placeholder="Search by name, CNIC, or card number..."
                    class="w-full pl-10 pr-3 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all"
                  />
                </div>
              </div>

              <div>
                <label
                  for="statusFilter"
                  class="block text-sm font-medium text-gray-700 mb-2"
                  >Card Status</label
                >
                <div class="relative">
                  <div
                    class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
                  >
                    <i class="fas fa-tag text-gray-400 text-sm"></i>
                  </div>
                  <select
                    id="statusFilter"
                    class="w-full pl-10 pr-3 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all appearance-none bg-white"
                  >
                    <option value="">All Statuses</option>
                    <option value="ACTIVE">Active</option>
                    <option value="INACTIVE">Inactive</option>
                    <option value="LOST">Lost</option>
                    <option value="DAMAGED">Damaged</option>
                  </select>
                  <div
                    class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none"
                  >
                    <i class="fas fa-chevron-down text-gray-400 text-sm"></i>
                  </div>
                </div>
              </div>

              <div>
                <label
                  for="limitSelect"
                  class="block text-sm font-medium text-gray-700 mb-2"
                  >Items Per Page</label
                >
                <div class="relative">
                  <div
                    class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
                  >
                    <i class="fas fa-list text-gray-400 text-sm"></i>
                  </div>
                  <select
                    id="limitSelect"
                    class="w-full pl-10 pr-3 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all appearance-none bg-white"
                  >
                    <option value="10">10</option>
                    <option value="25">25</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                  </select>
                  <div
                    class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none"
                  >
                    <i class="fas fa-chevron-down text-gray-400 text-sm"></i>
                  </div>
                </div>
              </div>

              <div class="flex items-end">
                <button
                  id="refreshBtn"
                  class="w-full bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-6 py-3 rounded-xl hover:from-cyan-600 hover:to-blue-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 flex items-center justify-center gap-2"
                >
                  <i class="fas fa-sync-alt text-sm"></i>
                  <span>Refresh Data</span>
                </button>
              </div>
            </div>
          </div>

          <!-- Cards Table -->
          <div class="glass-card rounded-2xl overflow-hidden">
            <div class="p-6 lg:p-8">
              <div class="flex justify-between items-center mb-6">
                <div class="flex items-center gap-3">
                  <div
                    class="p-2 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-lg text-white"
                  >
                    <i class="fas fa-table text-sm"></i>
                  </div>
                  <h2 class="text-xl font-bold text-gray-900">Cards Management</h2>
                </div>
                <div class="text-sm text-gray-600" id="resultsInfo">
                  Loading...
                </div>
              </div>

            <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Card Details
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Person
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Category
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Status
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      QR Code
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Issued Date
                    </th>
                    <th
                      class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                    >
                      Actions
                    </th>
                  </tr>
                </thead>
                <tbody
                  id="cardsTableBody"
                  class="bg-white divide-y divide-gray-200"
                >
                  <!-- Table rows will be dynamically populated -->
                </tbody>
              </table>
            </div>

            <!-- Loading State -->
            <div id="loadingState" class="text-center py-8">
              <i class="fas fa-spinner fa-spin text-2xl text-gray-400"></i>
              <p class="text-gray-600 mt-2">Loading cards...</p>
            </div>

            <!-- Empty State -->
            <div id="emptyState" class="text-center py-8 hidden">
              <i class="fas fa-id-card text-4xl text-gray-300"></i>
              <p class="text-gray-600 mt-2">No cards found</p>
            </div>
          </div>

          <!-- Pagination -->
          <div class="bg-gray-50 px-6 py-3 border-t border-gray-200">
            <div class="flex items-center justify-between">
              <div class="text-sm text-gray-600" id="paginationInfo">
                <!-- Pagination info will be dynamically populated -->
              </div>
              <div class="flex space-x-2" id="paginationControls">
                <!-- Pagination controls will be dynamically populated -->
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>

  <!-- Enhanced Modal System -->
  <%- include('partials/enhanced-modal') %>
  <%- include('modals/card-details-modal') %>
  <%- include('modals/create-card-modal') %>
  <%- include('modals/card-status-modal') %>
  <%- include('modals/qr-viewer-modal') %>

    <!-- Toast Notification -->
    <%- include('partials/toast') %>

    <script src="/js/modal-manager.js"></script>
    <script src="/js/card-management.js"></script>
  </body>
</html>
