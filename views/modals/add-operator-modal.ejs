<div
  id="add-operator-modal"
  class="fixed inset-0 bg-black bg-opacity-60 hidden z-50 backdrop-blur-sm modal-overlay flex items-center justify-center"
>
  <div
    class="bg-white rounded-3xl shadow-2xl max-w-md w-full mx-4 max-h-[95vh] overflow-hidden modal-container modal-md"
  >
    <div
      class="bg-gradient-to-r from-blue-50 to-cyan-50 px-8 py-6 border-b border-blue-100"
    >
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-4">
          <div
            class="w-12 h-12 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-2xl flex items-center justify-center shadow-lg"
          >
            <i class="fas fa-user-shield text-white text-lg"></i>
          </div>
          <div>
            <h2 class="text-2xl font-bold text-gray-900">Add New Operator</h2>
            <p class="text-sm text-gray-600">Create a new operator account</p>
          </div>
        </div>
        <button
          id="close-add-modal-btn"
          class="text-gray-400 hover:text-gray-600 transition-colors p-2 hover:bg-white hover:bg-opacity-50 rounded-xl"
        >
          <i class="fas fa-times text-xl"></i>
        </button>
      </div>
    </div>
    <div class="modal-body p-8 overflow-y-auto max-h-[70vh]">
      <form
        id="add-operator-form"
        action="/dashboard/operator/add"
        method="POST"
        class="space-y-4 lg:space-y-6"
      >
        <div>
          <label
            for="add-name"
            class="block text-sm font-semibold text-gray-700 mb-2"
          >
            <i class="fas fa-user mr-2 text-blue-600"></i>Full Name*
          </label>
          <input
            type="text"
            id="add-name"
            name="name"
            placeholder="Enter full name"
            required
            class="w-full px-3 lg:px-4 py-2 lg:py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 text-sm lg:text-base"
          />
        </div>
        <div>
          <label
            for="add-cnic"
            class="block text-sm font-semibold text-gray-700 mb-2"
          >
            <i class="fas fa-id-card mr-2 text-blue-600"></i>CNIC Number*
          </label>
          <input
            type="text"
            id="add-cnic"
            name="cnic"
            placeholder="Enter 13-digit CNIC number"
            pattern="\d{13}"
            maxlength="13"
            required
            class="w-full px-3 lg:px-4 py-2 lg:py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 font-mono text-sm lg:text-base"
          />
          <p class="mt-1 text-xs text-gray-500">
            Format: 1234567890123 (without dashes)
          </p>
        </div>
        <div>
          <label
            for="add-password"
            class="block text-sm font-semibold text-gray-700 mb-2"
          >
            <i class="fas fa-lock mr-2 text-blue-600"></i>Default Password*
          </label>
          <input
            type="password"
            id="add-password"
            name="password"
            placeholder="Enter default password"
            required
            class="w-full px-3 lg:px-4 py-2 lg:py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all duration-200 text-sm lg:text-base"
          />
        </div>
        <div class="flex space-x-3 pt-4">
          <button
            type="submit"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg transition-colors duration-200 flex items-center justify-center text-sm lg:text-base"
          >
            <i class="fas fa-save mr-2"></i>Add Operator
          </button>
          <button
            type="button"
            id="close-add-modal-btn"
            class="bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-2 px-4 rounded-lg transition-colors duration-200 flex items-center justify-center text-sm lg:text-base"
          >
            <i class="fas fa-times mr-2"></i>Cancel
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
